pnpm run lint:staged
pnpm run check:font
pnpm run check:types
pnpm run check:deps:commit

# Check token consistency when tokens.css is staged
if git diff --cached --name-only | grep -q 'src/styles/tokens.css'; then
    node scripts/check-token-consistency.mjs
fi
